# Core dependencies
opencv-python==4.9.0.80
numpy<2
face_recognition==1.3.0
face_recognition_models==0.3.0
setuptools<81

# Note: dlib is NOT listed here because it must be installed manually
# on Windows using a precompiled wheel matching your Python version.
# See README.md "Windows dlib install" section.